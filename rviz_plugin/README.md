# Descrete Event System Configurator 

> Rviz2 Plugin


## Colcon build with dependencies

Run `colcon build` to generate `compile_commands.json` used for clangd.

```sh
colcon build --cmake-args -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
```
